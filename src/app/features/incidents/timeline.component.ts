import { Component } from '@angular/core';import { DatePipe } from '@angular/common';import { DemoDataService } from '../../core/services/demo-data.service';
@Component({selector:'sai-timeline',imports:[DatePipe],template:`<header class="page-heading"><div><p class="eyebrow">Lewisville Tornado Response</p><h1>Incident Activity Timeline</h1><p>Auditable operational activity from the demonstration event stream.</p></div></header><section class="panel">@for(e of data.timeline();track e.id){<div class="timeline-row detailed"><time>{{e.at|date:'short'}}</time><span></span><div><small>{{e.type}}</small><h2>{{e.title}}</h2><p>{{e.detail}}</p><b>{{e.actor}}</b></div></div>}</section>`})export class TimelineComponent{constructor(readonly data:DemoDataService){}}
