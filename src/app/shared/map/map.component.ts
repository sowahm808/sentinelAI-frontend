import { Component,ElementRef,Input,OnDestroy,OnInit,ViewChild,signal } from '@angular/core';import { DemoDataService } from '../../core/services/demo-data.service';
type LayerKey='weather'|'hospitals'|'shelters'|'traffic'|'closures'|'resources';
@Component({selector:'sai-map',template:`<div class="map-shell" [class.compact]="compact"><div class="map-toolbar" aria-label="Map tools"><button (click)="layerOpen.set(!layerOpen())" [attr.aria-expanded]="layerOpen()">☷ Layers</button><button aria-label="Fit map to incident">⌖ Fit incident</button><button aria-label="Locate me" (click)="locate()">◎ Locate</button><button aria-label="Toggle basemap">◇ Basemap</button></div>@if(layerOpen()){<fieldset class="layer-panel"><legend>Operational layers</legend>@for(layer of layerList;track layer.key){<label><input type="checkbox" [checked]="layers()[layer.key]" (change)="toggle(layer.key)"><span>{{layer.icon}}</span>{{layer.label}}</label>}</fieldset>}<div class="map-canvas" #canvas aria-label="Interactive incident map" role="application"><div class="map-grid"></div><div class="impact-zone"></div>@if(layers().weather){<div class="weather-zone"></div>}@for(h of data.hospitals();track h.id){@if(layers().hospitals){<button class="marker hospital" [style.left.%]="25+$index*8" [style.top.%]="60-$index*5" (click)="select('Hospital',h.name,h.status+' · '+h.availableBeds+' beds available')" [attr.aria-label]="'Hospital: '+h.name">✚</button>}}@for(s of data.shelters().slice(0,6);track s.id){@if(layers().shelters){<button class="marker shelter" [style.left.%]="18+$index*11" [style.top.%]="35+$index%2*15" (click)="select('Shelter',s.name,(s.capacity-s.occupancy)+' spaces available')" [attr.aria-label]="'Shelter: '+s.name">⌂</button>}}@for(i of data.activeIncidents();track i.id){<button class="marker incident" [style.left.%]="48+$index*14" [style.top.%]="42+$index*12" (click)="select(i.type,i.title,i.severity+' · '+i.status)" [attr.aria-label]="'Incident: '+i.title">◆</button>}@if(layers().closures){@for(x of [1,2,3,4,5];track x){<span class="closure" [style.left.%]="15+x*13" [style.top.%]="20+x*9" title="Road closure">⊘</span>}}<div class="map-attribution">MapLibre-ready · fallback operational canvas</div></div><div class="legend"><b>Legend</b><span>◆ Incident</span><span>✚ Hospital</span><span>⌂ Shelter</span><span>⊘ Closure</span></div>@if(selected()){<aside class="entity-panel" aria-live="polite"><button (click)="selected.set(null)" aria-label="Close detail panel">×</button><span>{{selected()!.type}}</span><h3>{{selected()!.name}}</h3><p>{{selected()!.detail}}</p><small>Demonstration data · updated moments ago</small></aside>}</div>`})
export class MapComponent implements OnInit,OnDestroy{@Input()compact=false;@ViewChild('canvas')canvas?:ElementRef<HTMLElement>;readonly layerOpen=signal(false);readonly selected=signal<{type:string;name:string;detail:string}|null>(null);readonly layers=signal<Record<LayerKey,boolean>>({weather:true,hospitals:true,shelters:true,traffic:true,closures:true,resources:true});readonly layerList:{key:LayerKey;label:string;icon:string}[]=[{key:'weather',label:'Weather warnings',icon:'ϟ'},{key:'hospitals',label:'Hospitals',icon:'✚'},{key:'shelters',label:'Shelters',icon:'⌂'},{key:'traffic',label:'Traffic',icon:'≋'},{key:'closures',label:'Road closures',icon:'⊘'},{key:'resources',label:'Response resources',icon:'▣'}];constructor(readonly data:DemoDataService){}ngOnInit(){}ngOnDestroy(){}toggle(key:LayerKey){this.layers.update(v=>({...v,[key]:!v[key]}))}select(type:string,name:string,detail:string){this.selected.set({type,name,detail})}locate(){if(!navigator.geolocation)return;navigator.geolocation.getCurrentPosition(()=>this.select('Your location','Location available','Position is not retained by SentinelAI.'),()=>this.select('Location unavailable','Permission or signal unavailable','You can continue using the operational map.'))}}
